# 클래스 생성방법

class Cookie: # class 라고 적어주고 변수 첫 글자는 대문자로 해준다. 그리고 :(콜론)으로 구분해주고 생성 해준다.
    pass

초코쿠키 = Cookie() # 이런식으로 클래스를 변수에 담아 객체를 생성해준다.
아몬드쿠키 = Cookie()

# 초코쿠키와 아몬드쿠키는 같은 Cookie 모양을 갖지만 각각 다른 객체로 관리된다.

"""
객체와 인스턴스 차이??

class 로 생성한 객체를 인스턴스 라고도 한다.
그럼 객체와 인스턴스의 차이는 무었일까??

초코쿠키는 객체이다.
그리고 초코쿠키 객체는 Cookie의 인스턴스 이다.

즉 어미에 따라 다른 정의 차이일뿐 같은 의미이다.
초코쿠키 객체 이고 
초코쿠키는 Cookie의 인스턴스 이다
라고 한다.

하지만 크게 구분없이 사용하긴한다.
"""